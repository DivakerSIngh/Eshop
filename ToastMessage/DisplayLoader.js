 

        function DisplayProgress() {
            document.getElementById('pnlProgress').style.visibility = 'visible';
            window.setTimeout(HideProgressPanel, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressPanel() {
            document.getElementById('pnlProgress').style.visibility = "hidden", 50000
        }

        function DisplayProgress2() {
            document.getElementById('pnlProgress2').style.visibility = 'visible';
            window.setTimeout(HideProgressPanel2, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressPanel2() {
            document.getElementById('pnlProgress2').style.visibility = "hidden", 50000
        }

        function DisplayProgress3() {
            document.getElementById('pnlProgress3').style.visibility = 'visible';
            window.setTimeout(HideProgressPanel3, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressPanel3() {
            document.getElementById('pnlProgress3').style.visibility = "hidden", 50000
        }

        function DisplayProgress4() {
            document.getElementById('pnlProgress4').style.visibility = 'visible';
            window.setTimeout(HideProgressPanel4, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressPanel4() {
            document.getElementById('pnlProgress4').style.visibility = "hidden", 50000
        }
 
 //Campus Dropdown ProgressBar...........................................................................................................

        function ProgressCampus() {
            document.getElementById('pnlCampus').style.visibility = 'visible';
            window.setTimeout(HideProgressCampus, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressCampus() {
            document.getElementById('pnlCampus').style.visibility = "hidden", 50000
        }

 //Department Dropdown ProgressBar...........................................................................................................

        function ProgressDepartment() {
            document.getElementById('pnlDepartment').style.visibility = 'visible';
            window.setTimeout(HideProgressDepartment, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressDepartment() {
            document.getElementById('pnlDepartment').style.visibility = "hidden", 50000
        }
   
//University Dropdown ProgressBar...........................................................................................................

        function ProgressUniversity() {
            document.getElementById('pnlUniversity').style.visibility = 'visible';
            window.setTimeout(HideProgressUniversity, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressUniversity() {
            document.getElementById('pnlUniversity').style.visibility = "hidden", 50000
        }

 //Course Dropdown ProgressBar...........................................................................................................

        function ProgressCourse() {
            document.getElementById('pnlCourse').style.visibility = 'visible';
            window.setTimeout(HideProgressCourse, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressCourse() {
            document.getElementById('pnlCourse').style.visibility = "hidden", 50000
        }

//Sem Dropdown ProgressBar...........................................................................................................

        function ProgressSem() {
            document.getElementById('pnlSem').style.visibility = 'visible';
            window.setTimeout(HideProgressSem, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressSem() {
            document.getElementById('pnlSem').style.visibility = "hidden", 50000
        }

//Section Dropdown ProgressBar...........................................................................................................

        function ProgressSection() {
            document.getElementById('pnlSection').style.visibility = 'visible';
            window.setTimeout(HideProgressSection, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressSection() {
            document.getElementById('pnlSection').style.visibility = "hidden", 50000
        }

//Subject Dropdown ProgressBar...........................................................................................................

        function ProgressSubject() {
            document.getElementById('pnlSubject').style.visibility = 'visible';
            window.setTimeout(HideProgressSubject, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressSubject() {
            document.getElementById('pnlSubject').style.visibility = "hidden", 50000
        }


        //Session Dropdown ProgressBar...........................................................................................................

        function ProgressSession() {
            document.getElementById('pnlSession').style.visibility = 'visible';
            window.setTimeout(HideProgressSession, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressSession() {
            document.getElementById('pnlSession').style.visibility = "hidden", 50000
        }

//StudySession Dropdown ProgressBar...........................................................................................................

        function ProgressStudySession() {
            document.getElementById('pnlStudySession').style.visibility = 'visible';
            window.setTimeout(HideProgressStudySession, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressStudySession() {
            document.getElementById('pnlStudySession').style.visibility = "hidden", 50000
        }

//ReprtType Dropdown ProgressBar...........................................................................................................

        function ProgressReprtType() {
            document.getElementById('pnlReprtType').style.visibility = 'visible';
            window.setTimeout(HideProgressReprtType, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressReprtType() {
            document.getElementById('pnlReprtType').style.visibility = "hidden", 50000
        }

 //Student Dropdown ProgressBar...........................................................................................................

        function ProgressStudent() {
            document.getElementById('pnlStudent').style.visibility = 'visible';
            window.setTimeout(HideProgressStudent, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressStudent() {
            document.getElementById('pnlStudent').style.visibility = "hidden", 50000
        }

//LectureType Dropdown ProgressBar...........................................................................................................

        function ProgressLectureType() {
            document.getElementById('pnlLectureType').style.visibility = 'visible';
            window.setTimeout(HideProgressLectureType, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressLectureType() {
            document.getElementById('pnlLectureType').style.visibility = "hidden", 50000
        }
//TuteClass Dropdown ProgressBar...........................................................................................................

        function ProgressTuteClass() {
            document.getElementById('pnlTuteClass').style.visibility = 'visible';
            window.setTimeout(HideProgressTuteClasse, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressTuteClasse() {
            document.getElementById('pnlTuteClass').style.visibility = "hidden", 50000
        }

//LabGroup Dropdown ProgressBar...........................................................................................................

        function ProgressLabGroup() {
            document.getElementById('pnlLabGroup').style.visibility = 'visible';
            window.setTimeout(HideProgressLabGroup, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressLabGroup() {
            document.getElementById('pnlLabGroup').style.visibility = "hidden", 50000
        }

 //Month Dropdown ProgressBar...........................................................................................................

        function ProgressMonth() {
            document.getElementById('pnlMonth').style.visibility = 'visible';
            window.setTimeout(HideProgressMonth, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressMonth() {
            document.getElementById('pnlMonth').style.visibility = "hidden", 50000
        }

        //StudySession Dropdown ProgressBar...........................................................................................................

        function ProgressStudySession2() {
            document.getElementById('pnlStudySession2').style.visibility = 'visible';
            window.setTimeout(HideProgressStudySession2, 50000);  //handles hiding the progress panel should the operation time out
        }

        function HideProgressStudySession2() {
            document.getElementById('pnlStudySession2').style.visibility = "hidden", 50000
        }
       
